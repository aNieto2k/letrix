# Archivo de variables de entorno para DESARROLLO (Vite)
# Importante: Vite solo expone a cliente variables que empiecen por "VITE_"
# Tras cambiar cualquier variable, REINICIA el servidor de desarrollo.

# VITE_SEED_SALT
# - Sal opcional para la seed diaria de letras.
# - Cambiar este valor altera las letras generadas para un mismo día.
# - Útil en producción para controlar/rotar combinaciones diarias.
# - Nota: si ya jugaste hoy, el juego carga el estado guardado; borra la clave
#   del día en localStorage (o ejecuta localStorage.clear()) para ver el cambio.
#   Ejemplo en consola: localStorage.removeItem('wordgame_state_' + new Date().toDateString())
VITE_SEED_SALT=

# HIDE_DEBUG
# - Si es "1" o "true", NO se expone el objeto global window.debug.
# - Útil para ocultar utilidades de depuración en demos o entornos controlados.
# - Puedes sobreescribirlo en ejecución (HIDE_DEBUG=1 npm run dev) o aquí.
HIDE_DEBUG=0

# Notas adicionales:
# - En producción usa .env.production (o variables del entorno del servidor de build).
# - Recuerda que .env.local NO debe versionarse; ya está en .gitignore.
